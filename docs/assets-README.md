# Assets

The active project asset directory is `assets/`. It is no longer empty.
See `assets/README.md` for the current contents and ownership rules.

This compatibility document remains only because older project notes link to
`docs/assets-README.md`. New documentation should link directly to
`assets/README.md`.
