# assets/

Empty as of 2026-07-01.

Reserved for static files the shell needs at runtime — custom icons,
images, fonts not already available system-wide. Nothing needed here
yet since the only widget that exists (`widgets/TopBar/`) doesn't
render any icons or images.
