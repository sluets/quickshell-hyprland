-- MANAGED FILE — generated by the shell's ConfigManager
-- (settings window -> Hyprland page). Do NOT hand-edit: the next
-- settings Apply overwrites this file completely.
-- Managed values: general.gaps_in, general.gaps_out,
-- general.border_size, decoration.rounding.
-- Everything ELSE from the old general/decoration blocks (colors,
-- blur, shadow, opacity, layout...) lives in user/look.lua — yours.

hl.config({
    general = {
        gaps_in     = 5,
        gaps_out    = 10,
        border_size = 2,
    },
    decoration = {
        rounding = 10,
    },
})
