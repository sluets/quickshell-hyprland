NEXT SESSION — 2026-07-17

The current project should be refreshed from Git before making more edits.

Approved tonight:
- SDDM Phases 0–4 and manual Settings apply pipeline.
- Border-color linkage fix.
- Machine-specific Hyprland setup warning.
- Silent automatic backup pruning.
- Desktop clock center offsets, weather/temperature toggles, overall scale, shadow strength, and shadow X/Y offsets.
- Settings QML minimum size reduction.
- Hyprland rule forcing Quickshell Settings to float, center, and size at 1440x820.
- Native Super+M exit binding: hl.dsp.exit().

Important:
- Apply the Settings window rule separately on the laptop's rules.lua. See docs/HYPRLAND_WINDOW_RULES.md.
- Use the newest Git checkout as the parent. Do not reuse any earlier SettingsWindow.qml revision ZIP.
- The final tested Settings rule is:

hl.window_rule({
    name = "quickshell-settings",
    match = { class = "org.quickshell", title = "Quickshell Settings" },
    float = true,
    center = true,
    size = "1440 820",
})
